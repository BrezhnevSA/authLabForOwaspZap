import React,{useState} from 'react'; import {createRoot} from 'react-dom/client';
function App(){const [u,setU]=useState('');const [p,setP]=useState('');const [msg,setMsg]=useState('');async function submit(e){e.preventDefault();const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})});if(r.ok)location.href='/private';else setMsg('bad credentials')};return <main><h1>React JSON Login</h1><p>{msg}</p><form onSubmit={submit}><label>Username <input name="username" autoComplete="username" value={u} onChange={e=>setU(e.target.value)}/></label><br/><label>Password <input name="password" type="password" autoComplete="current-password" value={p} onChange={e=>setP(e.target.value)}/></label><br/><button>Sign in</button></form></main>}createRoot(document.getElementById('root')).render(<App/>);
